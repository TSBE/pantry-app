---
name: runner test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maui-build:
    runs-on: [self-hosted, macOS]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Prepare NuGet packages
        shell: bash
        run: |
          echo "The runners name is: $RUNNER_NAME"
